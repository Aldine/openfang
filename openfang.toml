# Local Docker configuration for OpenFang
api_listen = "0.0.0.0:50051"

[default_model]
provider = "openrouter"
model = "openrouter/auto"
api_key_env = "OPENROUTER_API_KEY"

[memory]
decay_rate = 0.05

[network]
listen_addr = "127.0.0.1:50051"
